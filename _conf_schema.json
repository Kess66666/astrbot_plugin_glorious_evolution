{
  "embedding_provider_id": {
    "type": "string",
    "description": "Embedding 供应商 ID",
    "hint": "填入 cmd_config.json 中的 embedding provider ID，如 Qwen/Qwen3-Embedding-8B 或 BAAI/bge-m3",
    "default": "Qwen/Qwen3-Embedding-8B"
  },
  "evolution_interval_hours": {
    "type": "int",
    "description": "进化间隔（小时）",
    "hint": "每多少小时自动执行一次记忆合并、洞察生成与淘汰",
    "default": 6,
    "slider": {
      "min": 1,
      "max": 72,
      "step": 1
    }
  }
}